{
  "name": "screeps-chinese-pack",
  "version": "1.0.0",
  "source": "src/main.ts",
  "main": "dist/群晖VideoStation时间跳转_解除右键屏蔽.user.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel --no-source-maps",
    "build": "rimraf ./.parcel-cache && parcel build --no-source-maps",
    "watch": "parcel watch --no-source-maps",
    "buildPlugin": "tsc --project ./plugin/src/tsconfig.json"
  },
  "devDependencies": {
    "@parcel/transformer-typescript-tsc": "^2.10.2",
    "parcel": "^2.10.2",
    "rimraf": "^5.0.5",
    "typescript": ">=3.0.0"
  }
}
